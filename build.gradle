subprojects {
  apply plugin: 'java'
  apply plugin: 'eclipse'

  version = '1.0'

  repositories {
    mavenCentral()
  }

  dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.+'
  }
}
